<?xml version="1.0"?>
<ruleset name="Laravel Task Management">
    <description>PSR-12 Coding Standard for Laravel Task Management System</description>

    <!-- Check all PHP files in the project -->
    <file>app</file>
    <file>config</file>
    <file>database</file>
    <file>routes</file>
    <file>tests</file>

    <!-- Exclude vendor and other generated files -->
    <exclude-pattern>*/cache/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>
    <exclude-pattern>*/*.blade.php</exclude-pattern>

    <!-- Use PSR-12 as the base standard -->
    <rule ref="PSR12"/>

    <!-- Show colors in output -->
    <arg name="colors"/>
    
    <!-- Show progress -->
    <arg value="p"/>
    
    <!-- Show sniff names in report -->
    <arg value="s"/>
</ruleset>

